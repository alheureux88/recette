# Project Instructions

## Git Workflow

**NEVER commit changes unless explicitly asked to do so.** 

Always make the changes and let the user review and test them first. Only commit when the user explicitly says "commit" or "commit these changes".
